# AI Personal Trainer

Plan-driven fitness coach: **React** + **Node.js** + on-device pose, with a **timer backbone** so sessions still run when the camera fails. The app generates or imports a plan (including rest times) and scores whether the user followed it.

Planning pack (feasibility, SRS, timeline, schema):

- [docs/README.md](./docs/README.md)

Implementation has not started. Build order is in the docs: schema and timers first, vision second.
